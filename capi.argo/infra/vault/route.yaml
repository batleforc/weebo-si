apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vault
  namespace: vault
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host("vault.capi.weebo.poc")
    services:
    - name: vault
      port: 8200
      scheme: https
