name: Upgrade helm

scms:
  default:
    kind: gitea
    spec:
      user: "{{ .github.user }}"
      email: "{{ .github.email }}"
      owner: "{{ .github.owner}}"
      repository: "{{ .github.repository}}"
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
      url: "https://git.batleforc.fr"
      branch: main

actions:
  default:
    kind: gitea/pullrequest
    scmid: default
    spec:
      title: "ci: Upgrade helm charts"
      automerge: false
      description: |
        Upgrade `helm` chart dependencies and container images in `2.argo` to the latest version.
      labels:
        - "Upgrade"
        - "UpdateCLI"

autodiscovery:
  scmid: default
  actionid: default
  crawlers:
    helm:
      rootdir: "2.argo"
