{{- if .Values.angos.enabled -}}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: angos-tls
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  secretName: angos-tls
  commonName: {{ .Values.angos.dns }}
  dnsNames:
    - {{ .Values.angos.dns }}
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  privateKey:
    size: 4096
  subject:
    organizationalUnits:
      - Infra
    organizations:
      - WeeboSI
  usages:
    - digital signature
    - key encipherment
{{- end -}}