{{- if .Values.batlehub.enabled -}}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: batlehub-valkey-cred
  namespace: {{ .Release.Namespace }}
spec:
  refreshPolicy: Periodic
  refreshInterval: 24h
  secretStoreRef:
    name: cred-store
  target:
    name: batlehub-valkey-cred
  data:
  - secretKey: pwd
    remoteRef:
      key: mv/batlehub/config
      property: pwd
  - secretKey: url
    remoteRef:
      key: mv/batlehub/config
      property: url
{{ end }}