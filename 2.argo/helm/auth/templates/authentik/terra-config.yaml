apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: terra-authentik
  namespace: argocd
spec:
  project: default
  source:
    path: 1.argo/terra/auth
    repoURL: "{{ .Values.repo }}"
    targetRevision: HEAD
  destination:
    namespace: auth
    server: "https://kubernetes.default.svc"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
