{{- if .Values.angos.enabled -}}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: angos
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: {{ .Values.angos.project }}
  source:
    repoURL: '{{ .Values.repo }}'
    path: '2.argo/helm/angos'
    targetRevision: '{{ .Values.branch }}'
    helm:
      releaseName: ingress
      valuesObject:
        project: "{{ .Values.angos.project }}"
        repo: "{{ .Values.repo }}"
        branch: "{{ .Values.branch }}"
        traefik:
          ips: "{{ .Values.traefik.ips }}"
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: 'angos'
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m
{{- end -}}